# Scheduler runtime
SCHEDULER_HOST=0.0.0.0
SCHEDULER_PORT=50051
NODE_ID=scheduler-1

# Consensus / replication settings
CONSENSUS_ENABLED=false
ETCD_ENDPOINTS=localhost:2379
ETCD_DIAL_TIMEOUT_SEC=2.0
LEASE_TTL_SEC=5
STATE_PREFIX=/edge-scheduler
